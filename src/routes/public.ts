import express from "express";

// This file is retained for backward-compatibility but all public endpoints
// have been moved into module-specific routers under `src/modules/*`.

const router = express.Router();

export default router;
